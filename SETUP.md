# 建筑工地考勤系统 · 设置说明(给非程序员)

这套系统一共 3 个文件:

| 文件 | 作用 |
|------|------|
| `index.html` | 整个考勤网页(手机打开就能用) |
| `supabase_setup.sql` | 在 Supabase 里建数据库、配安全规则的脚本 |
| `SETUP.md` | 就是本说明 |

跟着下面 5 步做完,大约 15 分钟就能上线。**不需要写任何代码。**

---

## 第 1 步:在 Supabase 建数据库

1. 打开 https://supabase.com/dashboard ,登录后点进你的项目。
2. 左侧菜单点 **SQL Editor(SQL 编辑器)**。
3. 点 **+ New query(新建查询)**。
4. 打开 `supabase_setup.sql` 文件,**全选、复制**里面所有内容,粘贴到中间的空白框里。
5. 点右下角 **Run(运行)**(或按 `Ctrl + Enter`)。
6. 下方出现绿色 **Success. No rows returned** 就说明成功了。✅

> 这一步建了 4 张表(工地、员工、用户资料、考勤),并配好了安全规则(RLS),
> 保证每个工地负责人只能看到自己工地的数据。

---

## 第 2 步:关闭"邮箱验证"(省去配邮件,强烈建议)

默认情况下,注册后 Supabase 会要求点邮件里的确认链接才能登录。为了简单,先关掉它:

1. 左侧菜单点 **Authentication(认证)**。
2. 点 **Sign In / Providers**(有些版本叫 **Providers**)。
3. 找到 **Email**,点开。
4. 把 **Confirm email(确认邮箱)** 的开关 **关掉**。
5. 点 **Save(保存)**。

> 以后注册的账号就能立即登录,不用收邮件了。
> (如果你更希望保留邮箱验证,可以不关,但注册后需要去邮箱点确认链接。)

---

## 第 3 步:把网页部署到 GitHub Pages

代码现在在分支 `claude/construction-attendance-system-b2424q` 上。要让 GitHub Pages 能访问,通常需要先合并到 `main` 分支:

1. 在 GitHub 仓库页面,把该分支 **合并(Merge)到 `main`**(通过一个 Pull Request,或直接在本地合并后推送)。
2. 打开仓库的 **Settings(设置)→ Pages**。
3. **Source(来源)** 选 **Deploy from a branch**。
4. **Branch(分支)** 选 **`main`**,文件夹选 **`/ (root)`**,点 **Save**。
5. 等 1~2 分钟,页面顶部会显示你的网址,类似:
   `https://你的用户名.github.io/attendance/`
6. 用手机浏览器打开这个网址,就能看到登录页了。📱

> 提示:`index.html` 必须在仓库**根目录**,GitHub Pages 才能自动找到它。本项目已经放好了。

---

## 第 4 步:创建"人事部"账号(你自己)

系统里所有人默认都是"工地负责人"角色。你需要把自己升级成"人事部"(管理员):

1. 打开上一步得到的网址。
2. 点下方 **"还没有账号?点这里注册"**。
3. 输入**你的邮箱**和一个**密码(至少 6 位)**,点 **注册**。
4. 提示注册成功后,回到 **Supabase → SQL Editor → New query**,粘贴下面这一行
   (把邮箱换成你刚才注册用的邮箱),点 **Run**:

   ```sql
   update public.profiles set role = 'hr' where email = '你的邮箱@example.com';
   ```

5. 回到网页,用同样的邮箱密码 **登录**,你就会看到「人事部」的界面了。🎉

---

## 第 5 步:日常使用

### 人事部(你)登录后可以:
- **工地与账号**:先添加各个工地(如"XX 路项目部")。
- **员工管理**:添加员工,并给每人选择所属工地;可停用/删除。
- **账号分配**:工地负责人在 App 注册后,会出现在「负责人账号 · 分配工地」里,
  给每个人选一个工地即可。
- **月度汇总**:选月份查看每位员工的出勤天数,点 **导出 CSV** 下载表格(用 Excel 打开中文不乱码)。

### 工地负责人登录后可以:
- 只看到**自己工地**的员工。
- 选日期(默认今天)→ 给每人点 **出勤 / 缺勤** → 点 **提交考勤**。
- 同一天可以随时改了再提交,以最后一次为准。

> **负责人怎么加进来?**
> 让每位工地负责人自己打开网址、点"注册"、用自己的邮箱密码注册。
> 然后你(人事部)在「工地与账号」页面给他分配工地。分配前他登录会提示"未分配工地"。

---

## 常见问题

**Q:负责人能看到别的工地吗?**
不能。安全规则(RLS)在数据库层面强制隔离,负责人无论如何都只能读到自己工地的员工和考勤。

**Q:这个 key 公开在网页里安全吗?**
安全。`publishable key`(也叫 anon key)本来就是设计给浏览器公开使用的,真正的权限控制靠数据库的 RLS 规则。**绝不要**把 `secret key`(service_role)放进网页——本项目也没有用到它。

**Q:忘记密码怎么办?**
在 Supabase → **Authentication → Users** 里找到该用户,可以重置密码;或删除后让他重新注册(注册后需重新分配工地)。

**Q:想换成自己的 Supabase 项目?**
打开 `index.html`,找到顶部这两行,换成你自己项目的地址和 publishable key 即可:
```js
const SUPABASE_URL = "https://xxxx.supabase.co";
const SUPABASE_KEY = "sb_publishable_xxxx";
```
(在 Supabase → **Project Settings → API** 里能找到这两个值。)

---

## 附:数据库脚本(与 `supabase_setup.sql` 内容一致)

如果你不方便打开文件,也可以直接复制下面全部内容粘贴到 Supabase 的 SQL Editor 运行,效果一样。

打开项目里的 `supabase_setup.sql` 文件复制即可 —— 内容较长,已单独存为文件,方便一键复制。
